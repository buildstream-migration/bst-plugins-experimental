# Pin coverage to 4.4.0 for now, we're experiencing random crashes with 4.4.42
coverage == 4.4.0
pytest-datafiles
pytest-env
pytest-cov
# Provide option to run tests in parallel, less reliable
pytest-xdist
pytest >= 3.1.0
pylint
pycodestyle
